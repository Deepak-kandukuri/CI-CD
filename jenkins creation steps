c7flexi server with 15gb volume


sudo apt update
sudo apt upgrade -y


sudo apt install openjdk-21-jdk -y

sudo apt install curl wget gnupg2 ca-certificates software-properties-common apt-transport-https -y

curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io-2026.key | \
sudo tee /usr/share/keyrings/jenkins-keyring.asc > /dev/null


echo "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] https://pkg.jenkins.io/debian-stable binary/" | \
sudo tee /etc/apt/sources.list.d/jenkins.list

sudo apt update

sudo apt install jenkins -y

sudo systemctl reset-failed jenkins
sudo systemctl daemon-reload
sudo systemctl enable jenkins
sudo systemctl start jenkins

sudo systemctl status jenkins


sudo cat /var/lib/jenkins/secrets/initialAdminPassword


docker pipeline and sonarqube scanner install

sudo apt update
sudo apt install docker.io -y
sudo usermod -aG docker ubuntu


generate token in sonarqube
manage/credentials/store/system/global/add credential/secret text
